# F1 Race Control Matrix - example ESPHome secrets
#
# Copy this file to the same ESPHome configuration directory as secrets.yaml,
# rename/copy it to secrets.yaml, and replace the placeholders below.
#
# NEVER commit your real secrets.yaml to Git.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
